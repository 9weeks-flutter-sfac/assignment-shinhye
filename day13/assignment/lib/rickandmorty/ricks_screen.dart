import 'package:flutter/material.dart';

class RicksScreen extends StatelessWidget {
  const RicksScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}
